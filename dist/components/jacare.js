/* Auto-generated by gulp-icon-font — Jacaré (.jcr) helpers
 * Pair with iconfont.css (or swfont.sprite.svg) in your Jacaré app.
 * Docs: https://github.com/jacarejs/core
 */
export const iconNames = [
  "fita-de-bookmark",
  "locked",
  "multiple-users-silhouette",
  "partilhar",
  "reiniciar",
  "shield",
  "snow-alt",
  "suporte-1",
  "users"
];

export const iconPrefix = 'swicon';

export function swIconClass(name) {
  return iconPrefix + '-' + name;
}

/*
Example SwIcon.jcr:

export <contract>
  props: {
    name: { type: 'string', required: true }
    label: { type: 'string', default: '' }
  }
</contract>

export <view>
  <i
    class=${() => swIconClass(typeof name === 'function' ? name() : name)}
    role="img"
    aria-label=${label}
    aria-hidden=${() => !(typeof label === 'function' ? label() : label)}
  ></i>
</view>
*/
